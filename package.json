{
  "name": "blocklist-ipsets",
  "version": "0.0.1",
  "description": "> Due to the amount of data and the frequency of the updates on this repo, > github has requested to limit the number of updates. > The site [https://iplists.firehol.org](https://iplists.firehol.org) has direct links > to all the files in this repo. **This repo is now updated once per day.**",
  "main": "index.js",
  "bin": {
    "blocklist-ipsets": "bin/gen_fw_user_rules.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cliff Cyphers",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
